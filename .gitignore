.DS_Store
.pytest_cache/
.venv/
__pycache__/
*.pyc
runtime_uploads/

# Ignore model checkpoints
image-classification-model-training/checkpoints/
image-segmentation-model-training/checkpoints/
*.pth

# Ignore training logs and TensorBoard artifacts
**/logs/**
**/*.log
**/events.out.tfevents.*
**/tfevents.*
logs_test/

# Environment variables
.env
# .env.local is developer-specific and must NOT be committed.
# .env.production IS committed so CI/CD uses the correct HF Space URL.
frontend/.env.local

# Docker local overrides (optional per-developer customisations)
docker-compose.override.yml

# agents
.agents
graphify-out/